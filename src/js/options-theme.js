/**
 * Options page theme dropdown listener
 * Sets up the theme dropdown to apply theme changes immediately
 */

// Call the setup function exported from theme-init.js
if (typeof window.setupThemeDropdownListener === 'function') {
    window.setupThemeDropdownListener();
}
